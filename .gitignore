node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example
.venv
__pycache__
*.pyc
.vite
/models/
*.safetensors
*.pt
data/realize/*.jsonl
!data/realize/.gitkeep


# local backups made by tools/clean_dictionary.py runs
tools/*.backup.json

# ONNX Runtime WASM, vendored from node_modules by scripts/copy-ort.mjs
# (~21 MB; regenerated by predev/prebuild, never committed)
public/ort/

# v0.2 parallel corpus (R-2.1b). Stats are committed, the JSONL is not.
data/parallel/*.jsonl
data/parallel/synthetic/*.jsonl

# Model predictions for scoring (M-10). Regenerated by tools/predict_onnx.mjs
# from the committed evaluation/test_sample.jsonl; the scores they produce are
# committed in evaluation/scores*.json, so the raw generations are not.
evaluation/predictions*.jsonl
